name: Build iOS — Restaurant

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: false
        default: 'archive'
        type: choice
        options:
          - archive

permissions:
  contents: write

jobs:
  build-ios:
    name: Build iOS Project
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build frontend (bundled into app)
        run: npm run build
        env:
          VITE_API_BASE_URL: https://olivepizza-owner.onrender.com
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_PROJECT_ID: olive-pizza-08
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}

      - name: Capacitor Sync iOS
        run: npx cap sync ios

      - name: Install CocoaPods (if Podfile exists)
        run: |
          cd ios/App
          if [ -f "Podfile" ]; then
            sudo gem install cocoapods
            pod install --repo-update
          else
            echo "Swift Package Manager project detected — skipping CocoaPods"
          fi

      - name: Select Xcode version
        run: sudo xcode-select -switch /Applications/Xcode.app

      - name: Build and Archive iOS
        run: |
          cd ios/App
          if [ -d "App.xcworkspace" ]; then
            TARGET="-workspace App.xcworkspace"
          else
            TARGET="-project App.xcodeproj"
          fi
          sudo gem install xcpretty || true
          xcodebuild \
            $TARGET \
            -scheme App \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath ${{ github.workspace }}/OlivePizza-Restaurant.xcarchive \
            archive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            PROVISIONING_PROFILE_SPECIFIER=""

      - name: Export Signed IPA (if signing secrets exist)
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_PROVISIONING_PROFILE_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          if [ -z "$APPLE_CERTIFICATE_BASE64" ] || [ -z "$APPLE_PROVISIONING_PROFILE_BASE64" ]; then
            echo "Signing secrets not configured — will package with Mach-O ad-hoc signature in next step."
            exit 0
          fi
          echo "Signing configured — exporting IPA..."
          echo "$APPLE_CERTIFICATE_BASE64" | base64 -d > /tmp/certificate.p12
          echo "$APPLE_PROVISIONING_PROFILE_BASE64" | base64 -d > /tmp/profile.mobileprovision
          security create-keychain -p "" build.keychain
          security import /tmp/certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -A
          security set-keychain-settings -t 3600 -u build.keychain
          security unlock-keychain -p "" build.keychain
          security list-keychains -d user -s build.keychain "$(security list-keychains -d user | head -1 | tr -d '"')"
          mkdir -p /tmp/ipa-export
          cat > /tmp/ExportOptions.plist << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>method</key><string>ad-hoc</string>
            <key>teamID</key><string>$APPLE_TEAM_ID</string>
            <key>uploadBitcode</key><false/>
            <key>compileBitcode</key><false/>
          </dict></plist>
          EOF
          xcodebuild \
            -exportArchive \
            -archivePath "${{ github.workspace }}/OlivePizza-Restaurant.xcarchive" \
            -exportPath /tmp/ipa-export \
            -exportOptionsPlist /tmp/ExportOptions.plist || true

      - name: Package Release Files
        run: |
          mkdir -p release-ios
          COMMIT_NUM=$(git rev-list --count HEAD)
          IPA_NAME="olivepizza-restaurant-${COMMIT_NUM}.ipa"
          ARCHIVE_PATH="${{ github.workspace }}/OlivePizza-Restaurant.xcarchive"
          if [ -d "$ARCHIVE_PATH" ]; then
            echo "Packaging unsigned IPA from archive..."
            mkdir -p /tmp/Payload
            cp -r "$ARCHIVE_PATH/Products/Applications/"*.app /tmp/Payload/ || true
            if [ -d "/tmp/Payload" ] && [ "$(ls -A /tmp/Payload)" ]; then
              echo "Applying ad-hoc signature to satisfy Mach-O binary requirements..."
              APP_BUNDLE=$(ls -d /tmp/Payload/*.app | head -n 1)
              echo "Packaging and signing app bundle: $APP_BUNDLE"
              # Ensure sound assets are packaged in app bundle
              cp -f ios/App/App/*.wav ios/App/App/*.mp3 "$APP_BUNDLE/" 2>/dev/null || true
              # Ensure GoogleService-Info.plist is packaged in app bundle (critical for Firebase iOS)
              cp -f ios/App/App/GoogleService-Info.plist "$APP_BUNDLE/" 2>/dev/null || true
              # 1. Sign embedded frameworks and dynamic libraries inside-out (critical: dyld aborts if frameworks lack code signature)
              if [ -d "$APP_BUNDLE/Frameworks" ]; then
                echo "Ad-hoc signing embedded frameworks..."
                find "$APP_BUNDLE/Frameworks" -depth -type d -name "*.framework" -exec codesign --force --sign - --timestamp=none {} + || true
                find "$APP_BUNDLE/Frameworks" -type f -name "*.dylib" -exec codesign --force --sign - --timestamp=none {} + || true
              fi
              # 2. Sign app extensions inside PlugIns (if any)
              if [ -d "$APP_BUNDLE/PlugIns" ]; then
                echo "Ad-hoc signing app extensions..."
                find "$APP_BUNDLE/PlugIns" -depth -type d -name "*.appex" -exec codesign --force --sign - --timestamp=none {} + || true
              fi
              # 3. Sign the main application bundle
              echo "Ad-hoc signing main bundle: $(basename "$APP_BUNDLE")"
              codesign --force --sign - --entitlements ios/App/App/App.entitlements --timestamp=none "$APP_BUNDLE" || true
              cd /tmp
              zip -r "${{ github.workspace }}/release-ios/${IPA_NAME}" Payload
              cd ${{ github.workspace }}
              echo "✅ Unsigned IPA created: ${IPA_NAME}"
            fi
          fi
          if [ -d "/tmp/ipa-export" ]; then
            cp -f /tmp/ipa-export/*.ipa "release-ios/${IPA_NAME}" || true
          fi
          ls -la release-ios/

      - name: Upload iOS Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: OlivePizza-Restaurant-iOS
          path: release-ios/*
          retention-days: 14

      # Publish to GitHub Release (Only Latest Commit Files)
      - name: Publish to GitHub Release
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
        run: |
          COMMIT_NUM=$(git rev-list --count HEAD)
          IPA_NAME="olivepizza-restaurant-${COMMIT_NUM}.ipa"
          echo "Publishing ${IPA_NAME} to release tag 'latest'..."

          # Ensure 'latest' release exists or create it
          gh release view latest --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1 || \
            gh release create latest --repo "${GITHUB_REPOSITORY}" --title "Olive Pizza Restaurant — Latest Release" --notes "Latest official builds for Olive Pizza Restaurant."

          # Only rotate assets if new IPA was successfully built
          if [ -f "release-ios/${IPA_NAME}" ]; then
            OLD_IOS=$(gh release view latest --repo "${GITHUB_REPOSITORY}" --json assets --jq '.assets[] | select(.name | test("(\\.(ipa|zip)$)|xcarchive")) | .name')
            for old in $OLD_IOS; do
              echo "Deleting old release asset: ${old}"
              gh release delete-asset latest "${old}" --repo "${GITHUB_REPOSITORY}" --yes || true
            done
            gh release upload latest "release-ios/${IPA_NAME}" --repo "${GITHUB_REPOSITORY}" --clobber
            echo "✅ Successfully uploaded ${IPA_NAME} to release 'latest'"
          else
            echo "⚠️ No IPA file generated at release-ios/${IPA_NAME} — preserving existing release assets."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build status summary
        run: |
          echo "## iOS Build Result" >> $GITHUB_STEP_SUMMARY
          if ls release-ios/*.ipa 2>/dev/null; then
            echo "**Status**: ✅ IOS_BUILD_READY" >> $GITHUB_STEP_SUMMARY
          else
            echo "**Status**: ⚠️ ARCHIVE_READY (xcarchive packaged)" >> $GITHUB_STEP_SUMMARY
          fi
