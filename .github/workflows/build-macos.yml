name: Build macOS — Restaurant

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci --legacy-peer-deps
      - name: Build frontend
        run: npm run build
        env:
          VITE_API_BASE_URL: https://olivepizza-owner.onrender.com
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_PROJECT_ID: olive-pizza-08
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
      - run: npx electron-builder --mac --config electron-builder.json
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
      - uses: actions/upload-artifact@v4
        with:
          name: OlivePizzaRestaurant-macOS-Installer
          path: dist-electron/*.dmg
          retention-days: 14
      # Create or Update Unified GitHub Release (All Platforms)
      - name: Create or Update Unified GitHub Release
        id: create_unified_release
        continue-on-error: true
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/main'
        with:
          name: "Olive Pizza Restaurant — Latest Release (All Platforms)"
          tag_name: "latest"
          files: dist-electron/*.dmg
          draft: false
          prerelease: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Create Versioned GitHub Release
      - name: Create Versioned GitHub Release
        id: create_release
        continue-on-error: true
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/main'
        with:
          name: "Olive Pizza Restaurant Manager macOS v1.0.${{ github.run_number }}"
          tag_name: "v1.0.${{ github.run_number }}-mac"
          files: dist-electron/*.dmg
          draft: false
          prerelease: false
          make_latest: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build status
        run: |
          if ls dist-electron/*.dmg 2>/dev/null; then
            echo "## macOS Build: MACOS_BUILD_READY — MACOS_SIGNING_NOT_CONFIGURED" >> $GITHUB_STEP_SUMMARY
          else
            echo "## macOS Build: MACOS_BUILD_FAILED" >> $GITHUB_STEP_SUMMARY
          fi
